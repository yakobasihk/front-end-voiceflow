import { Nullable } from '@voiceflow/api-sdk';
import { BaseEvent, BaseNode, BasePort, BaseStep, NodeID } from './types';
export declare const _V1_STOP_TYPES = "stopTypes";
export interface StepData<B = unknown> {
    _v: 1;
    payload: B;
    stop?: boolean;
    defaultPath?: number;
}
export interface Node<E = BaseEvent> extends BaseNode {
    _v: 1;
    payload: unknown;
    stop: boolean;
    defaultPath?: number;
    paths: Array<{
        event?: E;
        nextID: Nullable<NodeID>;
    }>;
}
export interface Step<B = unknown, E = BaseEvent> extends BaseStep<StepData<B>, BasePort<{
    event?: E;
}>[]> {
    type: string;
}
